<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:gravity="start">

    <!-- Profile title -->
    <TextView
        android:id="@+id/profile_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Profile"
        android:textSize="24sp"
        android:layout_marginBottom="16dp"/>

    <!-- Left-aligned profile message -->
    <TextView
        android:id="@+id/profile_message"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="You can access, update, or delete your profile at any time via the miniSASS website."
        android:textSize="16sp"
        android:layout_marginBottom="16dp"
        android:gravity="start" /> <!-- Left-aligned text -->

    <!-- You can add more profile-related elements here -->
</LinearLayout>
